apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    uid: loki  # <== This must match what you use in the dashboard JSON
    url: http://loki:3100
    isDefault: true
    editable: false
